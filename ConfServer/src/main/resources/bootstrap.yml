spring:
  application:
    name: configserver2
encrypt:
  failOnError: false
  keyStore:
    location: classpath:cloud.jks
    password: ${KEYSTORE_PASSWORD:testtest} # don't use a default in production
    alias: cloud
